<% if (post.tags && post.tags.length) { %>
  <span class="tags">taged in 
    <%- list_tags(post.tags, {
      show_count: false,
      class: "tag",
      style: 'none',
      separator: ', '
    }) %>
  </span>
<% } %>